Review the implementation in `$workspace` against the frozen rubric at `$rubric_path`.

The workspace identity and experimental treatment are intentionally hidden. Do not infer, discuss, or record treatment identity. Inspect only the implementation and the rubric. Do not modify production code, tests, or the rubric.

Write exactly one valid JSON score response for `$alias` with this shape:
{
  "schema_version": 1,
  "archetype": "nonempty architecture identifier from the rubric",
  "dimensions": {
    "behavior_preserved": true,
    "change_locality": true,
    "separation_of_concerns": true,
    "unnecessary_abstraction": true
  },
  "evidence": ["nonempty concrete file-and-symbol evidence"],
  "rationale": "nonempty explanation grounded in the evidence"
}

Use every dimension exactly once. `evidence` must be a nonempty array of nonempty strings. Do not add a preferred field; preferred status is derived from the frozen rubric. Base every conclusion on the frozen rubric and observed implementation only.